# v0.0.2

Switch package distribution to GitHub Packages under the `@liberte-top/*` owner-aligned scope.

## Notes

- rename `@liberte/shared` to `@liberte-top/shared`
- publish beta and stable tags to GitHub Packages
- keep package-aware docs and release gating unchanged
