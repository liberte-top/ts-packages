# v0.1.0

Move the shared Svelte component primitives out of the legacy `packages` repo and publish them from `ts-packages`.

## Included exports

- component entrypoint `@liberte-top/components`
- theme stylesheet `@liberte-top/components/theme.css`
